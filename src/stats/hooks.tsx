/*
export function useStats(): Stats {
  // TODO: actually needs to update
  const stats = useMemo(() => {
    return new Stats();
  }, []);

  useEffect(() => {
    document.body.appendChild(stats.dom);
  }, [stats.dom]);
  return stats;
}
*/
export function useStats() {}
