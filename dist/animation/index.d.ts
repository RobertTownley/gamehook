/// <reference types="react" />
interface Props {
    name: string;
    loop?: boolean;
    repetitions?: number;
}
export declare function Animation({ name, loop, repetitions }: Props): JSX.Element;
export {};
